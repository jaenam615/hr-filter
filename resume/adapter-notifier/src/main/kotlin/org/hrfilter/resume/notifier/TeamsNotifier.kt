package org.hrfilter.resume.notifier

internal class TeamsNotifier : Notifier {
    override fun notify(summary: BatchSummary) {
        TODO("MS Teams incoming webhook POST (okhttp). adaptive card payload")
    }
}
